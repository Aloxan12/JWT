import React from 'react';

interface IBurgerMenu{
    headers: any
    items: any
}

export const BurgerMenu = ({items, headers}:IBurgerMenu) => {
    return (
        <div>

        </div>
    );
};