import React from 'react';
import { AppCard } from '../../../shared/ui/AppCard/AppCard';
import { AppButton } from '../../../shared/ui/AppButton/AppButton';

export const ButtonsContainers = () => {
  return (
    <AppCard max title="Кнопки">
      <AppButton title="кнопка" />
    </AppCard>
  );
};
